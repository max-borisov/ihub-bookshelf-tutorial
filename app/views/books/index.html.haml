= render 'shared/flash_notice' if notice
= render 'shared/flash_alert' if alert
= render 'search'
.pull-left
.pull-right.book-count-block
  %p.text-info
    = pluralize(@books.count, 'book')
    in the catalog
.clearfix
- if @books.any?
  = will_paginate @books
  = render @books
  = will_paginate @books
-else
  .alert.alert-danger.alert-big.text-center No books found 